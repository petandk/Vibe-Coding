App(
    appid="key_spammer",
    name="Key Spammer",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="virtual_keyboard_app",
    requires=["gui"],
    stack_size=2 * 1024,
    order=20,
    fap_category="USB",
    fap_description="Spam a single character at configurable speed via USB HID",
    fap_author="User",
    fap_version="1.0",
    fap_icon="spammer.png",
)